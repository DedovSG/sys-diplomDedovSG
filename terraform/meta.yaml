#cloud-config

users:
 - name: dedovsg
   groups: sudo
   shell: /bin/bash
   sudo: ['ALL=(ALL) NOPASSWD:ALL']
   ssh-authorized-keys:
     - ssh-ed25519 
chpasswd:
  list: |
    dedovsg:sys-23
  expire: False

